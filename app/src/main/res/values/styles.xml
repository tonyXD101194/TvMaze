<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Text Views -->

    <style name="TitleTextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/title_show_item</item>
    </style>

    <style name="TitleTextViewBold" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/title_show_item</item>
        <item name="android:padding">@dimen/default_size</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="DescriptionTextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/title_show_item</item>
        <item name="android:padding">@dimen/default_size</item>
        <item name="android:gravity">start</item>
    </style>

    <style name="SummaryTextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">0dp</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/summary_show_item</item>
        <item name="android:padding">@dimen/default_size</item>
        <item name="android:gravity">top|start</item>
        <item name="android:scrollHorizontally">true</item>
        <item name="android:ellipsize">end</item>
    </style>

    <style name="ItemTextView" parent="Widget.MaterialComponents.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="android:textSize">@dimen/summary_show_item</item>
        <item name="android:padding">@dimen/default_size</item>
    </style>

    <style name="ErrorEmptyTextView" parent="Widget.AppCompat.TextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:drawableTop">@drawable/ic_error_80dp</item>
        <item name="android:padding">@dimen/default_size_x3</item>
        <item name="android:gravity">center</item>
    </style>

    <!-- Button -->

    <style name="ButtonAction" parent="Widget.AppCompat.Button">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:padding">@dimen/default_size</item>
        <item name="android:layout_margin">@dimen/default_size</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>

    <!-- Progress Bar -->
    <style name="ProgressLoading" parent="Widget.AppCompat.ProgressBar">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:padding">@dimen/default_size</item>
        <item name="android:indeterminate">true</item>
        <item name="android:indeterminateTint">@color/colorPrimary</item>
        <item name="android:indeterminateTintMode">src_atop</item>
    </style>

</resources>
